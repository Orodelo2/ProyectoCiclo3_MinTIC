@page
@model MyApp.Namespace.Directivos.ListModelDirectivo
@{
    ViewData["Title"] = "Lista de Directivos";
}
<div class="text-center center">
    <h1>Directivos</h1>
</div>

<br />

<div>
<td>
    <a class="btn btn-outline-primary" asp-page="./Create">Nuevo Directivo
        <i class="fas fa-user-plus"></i>
    </a>
</td>
</div>
<br />

<table class="table">
    <tr>
        <!-- <th>Id</th> -->
        <th>Tipo Doc</th>
        <th>Número</th>
        <th>Nombre</th>
        <th>Apellido</th>
        <th>Detalles</th>
        <th>Editar</th>        
        <th>Eliminar</th>
    </tr>
    @foreach (var directivo in Model.directivos)
    {
        <tr>
            <!--<td>@directivo.Id</td>-->
        <td>@directivo.TipoDocumento</td>
            <td>@directivo.NumeroDocumento</td>
            <td>@directivo.Nombre</td>
            <td>@directivo.Apellido</td>
            <td>
                <a class="btn btn-outline-info" asp-page="./Details" asp-route-id="@directivo.Id">
                    <i class="far fa-id-badge"></i>
                </a>

            </td>
            <td>
                <a class="btn btn-outline-secondary" asp-page="./Update" asp-route-id="@directivo.Id">
                    <i class="fas fa-user-edit"></i>
                </a>

            </td>
            <td>
                <a class="btn btn-outline-danger" asp-page="./Delete" asp-route-id="@directivo.Id">
                     <i class="fas fa-user-minus"></i>
                </a>

            </td>
        </tr>
    }

</table>
